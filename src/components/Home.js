import React from 'react';
import TextToSqlConverter from './TexttoSQL';

const Home = () => {
  return ( // Make sure to return the JSX
    <div className="container">

      <TextToSqlConverter/>
    </div>
  );
};

export default Home;
